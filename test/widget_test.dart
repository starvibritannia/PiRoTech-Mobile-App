// Test dasar PiRoTech.
// Test ini tidak me-route UI apapun — hanya memastikan paket bisa di-compile
// & menyiapkan kerangka untuk test asli di masa depan.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('PiRoTech smoke test', () {
    expect(1 + 1, 2);
  });
}